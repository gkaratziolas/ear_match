Works on Linux, not sure about Windows (maybe with sygwin or MinGW)


start by running:

> cmake .
> make

Once that's done, run the program with:

> ./ear_match

Controlls: 
<space> to pause/unpause
"a" to capture a model, based on the current ear in frame
"c" to quit